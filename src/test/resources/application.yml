datasources:
  default:
    url: jdbc:postgresql://localhost:5432/rawdata_converter_jobs
    username: boss
    password: "bossman"
    driverClassName: org.postgresql.Driver

flyway:
  datasources:
    default:
      enabled: true
